<mah:MetroWindow x:Class="wine_lite_view.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:local="clr-namespace:wine_lite_view.ViewModels"
        mc:Ignorable="d"
        Title="{Binding Title, Mode=OneWay}"
        Icon="{Binding Icon, Mode=OneWay}"
        Height="450"
        Width="800"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Name="MenuItemnew" Header="_New DB"/>
                <MenuItem Name="MenuItemOpen" Header="_Open DB"/>
                <Separator/>
                <MenuItem Name="MenuItemExit" Header="_Exit"/>
            </MenuItem>
        </Menu>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <Label Content="Opened File:" Margin="5 0"/>
                    <Label Content="{Binding CurrentDbPath, Mode=OneWay}" Margin="5 0"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
        <DockPanel DockPanel.Dock="Bottom">
            <mah:MetroTabControl>
                <mah:MetroTabItem>

                </mah:MetroTabItem>
            </mah:MetroTabControl>
        </DockPanel>
    </DockPanel>
</mah:MetroWindow>
